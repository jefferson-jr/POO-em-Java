class Personagem {
  constructor(nome, clone, poder, invocacao, velocidade){
    // atributos
    this.nome = nome;
    this.clone = clone;
    this.poder = poder;
    this.invocacaao = invocacao;
    this.velocidade = velocidade;
  }
  //Metodos
  poderEspecial() {
    console;log(` rasegan ${this.rasegan}!`);
  }
 invocacao() {
    log(`naruto invoca um sapo  ${this.invocacao}!`);
 }
     clone() {
    console;log(`naruto clona ele mesmo ${this.clone}!`);

     }
  }

